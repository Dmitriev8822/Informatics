#include <iostream>

using namespace std;

bool checkArray(int *p, int n)
{

}

int decomposition(int n, long long sum, int j)
{
    for(int i = 2; i <= n; i++)
    {
        if(n % i == 0)
        {
            if()

        }
    }
}

int main()
{
    int a, b, c;
    //cin >> a >> b >> c;
    a = 1234; b = 1233; c = 1235;
//    long long i = max(max(a,b), max(b,c));
//    while(i % a != 0 || i % b != 0 || i % c != 0)
//        i++;
//    cout << i;
    return 0;
}
