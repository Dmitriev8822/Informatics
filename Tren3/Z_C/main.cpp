#include <iostream>

using namespace std;

int main()
{
    int n;
    int m;
    cin >> n >> m;
    for(int i = 1; i <= m; i++)
    {
        for(int j = 1; j <= m; j++)
        {
            int sum = 0;
            for(int i = 3; i < n; i++)
            {
                sum +=
            }
                if(sum == m)
                {
                    cout << i << " " << j << endl;
                    return 0;
                }
        }
    }
    return 0;
}
